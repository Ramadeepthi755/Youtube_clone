You can see this youtube clone on clicking below link
https://youtubeclone755.netlify.app/

## API setup
1. Create a YouTube Data API v3 key in Google Cloud.
2. Paste the key into `app.js` (the `YT_API_KEY` constant).
3. Run from a local server if your API key is restricted by HTTP referrer.
